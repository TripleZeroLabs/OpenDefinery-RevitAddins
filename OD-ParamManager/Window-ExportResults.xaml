<Window x:Class="OD_ParamManager.Window_ExportResults"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Export Results"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="{DynamicResource OD.Brush.Background}"
        Width="860" Height="560"
        MinWidth="640" MinHeight="400"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/OpenDefinery.Theme;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeadingText"
                   Text="Export Complete"
                   Style="{StaticResource OD.Text.Heading}"/>

        <TextBlock Grid.Row="1" x:Name="SummaryText"
                   Margin="0,6,0,16"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource OD.Brush.TextSecondary}"/>

        <DataGrid Grid.Row="2" x:Name="ResultsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <!-- Status icon -->
                <DataGridTemplateColumn Header="" Width="46">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                       FontSize="16" FontWeight="Bold"
                                       ToolTip="{Binding OutcomeLabel}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Outcome}" Value="Added">
                                                <Setter Property="Text" Value="&#x2714;"/>
                                                <Setter Property="Foreground" Value="{DynamicResource OD.Brush.Success}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Outcome}" Value="AlreadyExists">
                                                <Setter Property="Text" Value="&#x2714;"/>
                                                <Setter Property="Foreground" Value="{DynamicResource OD.Brush.Primary}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Outcome}" Value="Failed">
                                                <Setter Property="Text" Value="&#x2716;"/>
                                                <Setter Property="Foreground" Value="{DynamicResource OD.Brush.Danger}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Parameter" Binding="{Binding Name}" Width="240"/>

                <DataGridTemplateColumn Header="GUID" Width="270">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Guid}" FontFamily="Consolas" FontSize="11"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Details" Binding="{Binding Message}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" x:Name="OkButton"
                Content="OK"
                Width="110"
                HorizontalAlignment="Right"
                Margin="0,20,0,0"
                IsDefault="True"
                Click="OkButton_Click"/>
    </Grid>
</Window>
